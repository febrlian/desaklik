version: "3.8"

# Deprecated for local development.
# Use ../docker-compose.yml at the repository root as the single source of truth.
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: always
